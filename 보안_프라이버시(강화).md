# 보안/프라이버시(강화)

## 동의 수명주기 요구사항

### 1) 동의 상태 모델 및 상태 전이 규칙

동의 상태는 아래 4가지로 모델링한다.

- **동의**: 해당 동의 항목에 대해 모든 필수 처리 목적/기능 사용을 허용한 상태
- **부분동의**: 동의 항목의 세부 목적 또는 기능 중 일부만 허용한 상태
- **철회**: 이용자가 기존 동의를 명시적으로 취소한 상태
- **만료**: 동의 유효기간 경과 또는 정책상 재동의 필요 시점 도달로 동의 효력이 종료된 상태

상태 전이 규칙은 다음과 같다.

- `초기` → `동의`: 최초 동의 획득(필수 고지/버전 기록 완료)
- `초기` → `부분동의`: 선택 항목 중 일부만 허용한 경우
- `동의` ↔ `부분동의`: 항목별 허용 범위 변경(확대/축소)
- `동의` → `철회`: 이용자 또는 법정대리인의 명시적 철회 요청
- `부분동의` → `철회`: 남은 허용 항목까지 전면 철회
- `동의`/`부분동의` → `만료`: 유효기간 만료, 약관·처리방침 중요 변경으로 재동의 필요 시
- `철회` → `동의`/`부분동의`: 재동의 완료 시에만 가능(기존 이력 유지, 신규 버전으로 기록)
- `만료` → `동의`/`부분동의`: 재동의 완료 시에만 가능
- `철회` ↔ `만료` 직접 전이는 금지한다. 만료 시점 이전 철회는 `철회`로 우선 기록한다.

추가 통제 규칙:

- 상태 전이 시점마다 **주체, 시각, 채널, 동의서 버전**을 감사로그에 의무 저장한다.
- `철회` 또는 `만료` 상태에서는 동의 기반 처리 기능을 기본 차단하고, 법적 의무 처리만 예외 허용한다.

### 2) 동의 항목별 허용 기능/차단 기능 매트릭스

| 동의 항목 | 상태 | 허용 기능 | 차단 기능 |
|---|---|---|---|
| A-01 기본 서비스 데이터 처리 | 동의 | 계정 운영, 기본 기록 조회/저장 | 없음 |
| A-01 기본 서비스 데이터 처리 | 부분동의 | 필수 기능만 허용, 선택 분석 기능 제한 | 선택형 부가 분석 |
| A-01 기본 서비스 데이터 처리 | 철회/만료 | 법적 보관·분쟁 대응 범위 내 최소 처리 | 신규 저장, 추천·분석 기능 |
| B-02 마케팅/알림 | 동의 | 이벤트 알림, 개인화 캠페인 발송 | 없음 |
| B-02 마케팅/알림 | 부분동의 | 선택 채널(예: 앱푸시만) 발송 | 미동의 채널 발송(SMS/이메일 등) |
| B-02 마케팅/알림 | 철회/만료 | 서비스 필수 공지(비마케팅)만 허용 | 모든 마케팅 메시지 발송 |
| C-05 검사수치 활용 | 동의 | 검사수치 기반 리포트/추천/추적 | 없음 |
| C-05 검사수치 활용 | 부분동의 | 비식별 통계 또는 제한된 리포트만 허용 | 고도화 추천, 외부연계 분석 |
| C-05 검사수치 활용 | 철회/만료 | 법정 보관 또는 분쟁 대응 목적 열람만 허용 | **C-05 기능 전반 제한**(개인 리포트 생성/추천/추세 비교) |
| D-07 제3자 제공 | 동의 | 고지된 제3자 제공 API 처리 | 없음 |
| D-07 제3자 제공 | 부분동의 | 특정 제3자/목적에 한해 제공 | 미동의 제3자 전송 |
| D-07 제3자 제공 | 철회/만료 | 법적 의무 제출 외 제공 금지 | 일반적 제3자 제공/연동 |

매트릭스 운영 규칙:

- 하나의 이용자에 대해 항목별 상태가 다를 수 있으며, 기능 게이트는 **항목 단위 상태**를 우선 참조한다.
- `부분동의`의 세부 허용 범위는 동의서 버전별 정책 테이블로 관리한다.
- `철회` 또는 `만료`가 적용된 항목은 배치/실시간 파이프라인 모두에서 차단되어야 한다.

### 3) 철회 시 기존 데이터 처리 정책(목적별 분리)

철회 발생 시 기존 데이터는 목적별로 다음 정책을 적용한다.

| 처리 목적 | 철회 후 정책 | 세부 기준 |
|---|---|---|
| 서비스 제공 이력/거래 증빙 | 보관 | 전자상거래·세무 등 법정 보존기간 동안 분리보관, 기간 만료 후 파기 |
| 보안·부정사용 탐지 | 보관(최소화) | 보안 로그는 최소 항목만 유지, 접근권한 제한 및 암호화 적용 |
| 통계/연구/모델 개선 | 익명화 | 개인 식별자 제거·비가역 처리 후에만 활용, 재식별 금지 |
| 개인화 추천/마케팅 | 삭제 | 철회 즉시 처리중지, 캐시·색인·파생테이블 포함 삭제 큐에 등록 |
| 고객지원/분쟁 대응 | 보관 | 접수 건 종결 및 법정 시효까지 보관, 이후 지체 없이 삭제 |

운영 요구사항:

- `보관/익명화/삭제` 판단은 단일 전역 규칙이 아니라 **처리 목적 단위 정책 엔진**으로 수행한다.
- 삭제는 논리삭제 후 물리삭제 완료 시점까지 추적 가능해야 하며, 백업본 반영 시점을 별도 관리한다.
- 익명화 처리 데이터는 원본 복구가 불가능해야 하며, 검증 로그를 남긴다.

### 4) 감사로그 필수 저장 항목 확장(동의 변경 이력)

감사로그에는 아래 동의 변경 이력을 필수 저장한다.

- **주체**: 이용자 본인/법정대리인/관리자/시스템 배치 식별자
- **시각**: 변경 요청 시각, 반영 완료 시각(UTC 및 로컬타임 동시 저장 권장)
- **채널**: 모바일 앱, 웹, 고객센터, 오프라인 서면, API 등
- **버전**: 동의서/처리방침/약관 버전 및 해시

권장 추가 필드:

- 변경 전 상태, 변경 후 상태, 동의 항목 코드
- 요청 원문/증빙 참조 ID, 처리 결과 코드, 실패 사유
- 작업자 또는 시스템 주체 인증수단(세션ID, 토큰 식별자 등)

감사로그 무결성 요구사항:

- 감사로그는 위변조 탐지를 위해 해시체인 또는 서명 기반 무결성 검증을 적용한다.
- 보존기간 동안 검색 가능해야 하며, 규제기관 제출용 내보내기를 지원한다.
